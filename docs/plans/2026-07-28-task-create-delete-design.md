# 施工任务新增与删除设计

## 目标

让用户在施工任务清单中直接发现新增入口，并能安全删除不再需要的任务。

## 交互

- 保留阶段概览中的新增入口，同时在任务清单标题旁增加就近的“新增任务”按钮。
- 每行将状态选择和删除图标组合为操作区，避免改变任务标题、优先级和日期的扫描顺序。
- 删除使用全局危险确认弹窗，显示任务名和所属阶段，并说明状态与日期会一并移除。
- 删除后沿用现有数据更新流程，自动写入本地缓存并同步到云端。

## 响应式

- 桌面端状态选择与删除图标保持同一行。
- 移动端操作区落在任务标题下方，状态选择占据剩余宽度，删除按钮保持稳定触控尺寸。
